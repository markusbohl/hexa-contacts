export const path = {
    ROOT: __dirname + "/../",
    DIST: __dirname + "/../dist/",
    SRC: __dirname + "/../src/"
};

export const task = {
    BUILD_TS: "build_ts",
    CLEAN_DIST: "clean:dist",
    WATCH: "watch",
    TEST: "test",
    TDD: "tdd"
};